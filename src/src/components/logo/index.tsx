import { FC } from 'react';

import './index.scss';

const Logo: FC = () => {
    return (
        <div className="blog-logo">
            <span>
                MEMOYU
                <small>'BLOG</small>
            </span>
        </div>
    );
};

export default Logo;
