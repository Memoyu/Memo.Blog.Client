import { FC } from 'react';

import './index.scss';

interface ComProps {}

const Index: FC<ComProps> = ({}) => {
    return (
        <div>
            组件
            <div>content</div>
        </div>
    );
};

export default Index;
